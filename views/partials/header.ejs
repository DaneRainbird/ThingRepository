<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">ThingRepository</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <% if (isAuthenticated) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/profile">Your Profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/things">Your Things</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout<%= activeRoute %>">Log Out</a>
                </li>
            <% } else { %> 
                <li class="nav-item">
                    <a class="nav-link" href="/login<%= activeRoute %>">Log In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/sign-up<%= activeRoute %>">Sign Up!</a>
                </li>
            <% } %>
        </ul>
        <ul class="navbar-nav ml-auto">
            <span class="navbar-text">
                Right aligned
            </span>
        </ul>
        <% if (isAuthenticated) { %>
            <span class="navbar-text">
                You are signed in as <%= user %>
            </span>
        <% } %>
    </div>
  </nav>